<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Backup Module Configuration
    |--------------------------------------------------------------------------
    |
    | Настройки модуля автоматических бекапов
    |
    */

    'enabled' => env('BACKUP_ENABLED', true),

    /*
    |--------------------------------------------------------------------------
    | Schedule Configuration
    |--------------------------------------------------------------------------
    |
    | Настройки расписания автоматических бекапов
    |
    */
    'schedule' => [
        'enabled' => true,
        'frequency' => env('BACKUP_FREQUENCY', 'daily'), // daily, weekly, monthly
        'time' => env('BACKUP_TIME', '03:00'), // Время запуска
        'day_of_week' => 0, // Для weekly (0=воскресенье, 1=понедельник и т.д.)
        'day_of_month' => 1, // Для monthly (1-31)
    ],

    /*
    |--------------------------------------------------------------------------
    | Backup Content
    |--------------------------------------------------------------------------
    |
    | Что включать в бекап
    |
    */
    'include' => [
        'database' => true,
        'files' => [
            'public/uploads',
            'public/images',
            'public/wp-content/uploads',
        ],
        'config' => true, // .env и важные конфиги
        'code' => false, // Весь код приложения (долго, не рекомендуется)
    ],

    /*
    |--------------------------------------------------------------------------
    | Exclusions
    |--------------------------------------------------------------------------
    |
    | Что исключать из бекапа
    |
    */
    'exclude' => [
        'storage/logs/*',
        'storage/framework/cache/*',
        'storage/framework/sessions/*',
        'storage/framework/views/*',
        'storage/backups/*', // Не бекапить сами бекапы
        '*.log',
        '.DS_Store',
        'Thumbs.db',
    ],

    /*
    |--------------------------------------------------------------------------
    | Storage Configuration
    |--------------------------------------------------------------------------
    |
    | Настройки хранилища бекапов
    |
    */
    'storage' => [
        'local' => [
            'enabled' => true,
            'path' => storage_path('app/backups'),
            'max_size' => 50 * 1024 * 1024 * 1024, // 50 GB максимум
        ],
        'remote' => [
            'enabled' => env('BACKUP_REMOTE_ENABLED', false),
            'driver' => env('BACKUP_REMOTE_DRIVER', 's3'), // s3, yandex, ftp
            'path' => env('BACKUP_REMOTE_PATH', 'backups/'),
            'auto_upload' => true, // Автоматически загружать на remote после создания
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | Retention Policy
    |--------------------------------------------------------------------------
    |
    | Политика хранения бекапов (ротация)
    |
    */
    'retention' => [
        'keep_last' => 7, // Хранить последние N бекапов всегда
        'keep_daily' => 7, // По одному за день (за последние N дней)
        'keep_weekly' => 4, // По одному за неделю (за последние N недель)
        'keep_monthly' => 6, // По одному за месяц (за последние N месяцев)
    ],

    /*
    |--------------------------------------------------------------------------
    | Database Configuration
    |--------------------------------------------------------------------------
    |
    | Настройки для дампа базы данных
    |
    */
    'database' => [
        'connection' => env('DB_CONNECTION', 'mysql'),
        'mysqldump_path' => env('BACKUP_MYSQLDUMP_PATH', 'mysqldump'), // Путь к mysqldump
        'compression' => true, // Сжимать SQL дамп (gzip)
        'exclude_tables' => [
            // Таблицы, которые не нужно бекапить
            'sessions',
            'cache',
            'cache_locks',
            'jobs',
            'failed_jobs',
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | Notification Settings
    |--------------------------------------------------------------------------
    |
    | Настройки уведомлений
    |
    */
    'notifications' => [
        'enabled' => env('BACKUP_NOTIFICATIONS_ENABLED', true),
        'email' => env('BACKUP_NOTIFICATION_EMAIL', env('ADMIN_EMAIL', 'admin@example.com')),
        'on_success' => false, // Email при успешном бекапе
        'on_failure' => true, // Email при ошибке
    ],

    /*
    |--------------------------------------------------------------------------
    | Rate Limiting
    |--------------------------------------------------------------------------
    |
    | Ограничения на частоту создания бекапов
    |
    */
    'rate_limit' => [
        'enabled' => true,
        'min_interval' => 300, // Минимум 5 минут между бекапами (в секундах)
    ],

    /*
    |--------------------------------------------------------------------------
    | Cleanup Settings
    |--------------------------------------------------------------------------
    |
    | Настройки автоматической очистки
    |
    */
    'cleanup' => [
        'auto_cleanup_enabled' => true, // Автоматически удалять старые бекапы
        'cleanup_schedule' => 'daily', // Как часто запускать cleanup
        'delete_failed_after_days' => 7, // Удалять ошибочные бекапы через N дней
    ],

];
