# ✅ Caption Shortcodes и Визуальный редактор - Реализовано

## 📋 Проблемы, которые были решены

### 1. ❌ Проблема с [caption] shortcodes

**Было:**
```
[caption id="attachment_14887" align="alignnone" width="1200"]
<img class="size-full wp-image-14887" src="/imgnews/1200x630_0xrzhcvcxt_1298651934854357840.webp" alt="" width="1200" height="630" />
Ваня Дмитриенко, фото: пресс-служба
[/caption]
```

Эти шорткоды не отображались правильно на сайте и выглядели как некрасивый текст.

**Стало:**
```html
<figure class="wp-caption alignnone" style="margin: 20px auto; text-align: center; max-width: 1200px;">
    <img style="width: 100%; height: auto; border-radius: 5px;" 
         class="size-full wp-image-14887" 
         src="/imgnews/1200x630_0xrzhcvcxt_1298651934854357840.webp" 
         alt="" 
         width="1200" 
         height="630" />
    <figcaption class="wp-caption-text" style="margin-top: 10px; font-size: 14px; color: #666; font-style: italic; line-height: 1.5;">
        Ваня Дмитриенко, фото: пресс-служба
    </figcaption>
</figure>
```

### 2. ❌ Отсутствие визуального редактора

**Было:**
- Простое textarea без форматирования
- Нужно было писать HTML вручную
- Неудобное редактирование текста
- Нет предпросмотра

**Стало:**
- Полноценный WYSIWYG редактор TinyMCE
- Русская локализация
- Визуальное форматирование текста
- Вставка изображений
- Предпросмотр
- Режим кода для продвинутых пользователей

---

## 🔧 Что было сделано

### 1. Обновлён ContentHelper.php

#### Добавлен метод `convertCaptionShortcode()`

Этот метод преобразует WordPress shortcode `[caption]` в красивый HTML с использованием семантических тегов `<figure>` и `<figcaption>`.

**Основные возможности:**

✅ **Парсинг параметров:**
- `id` - идентификатор вложения
- `align` - выравнивание (alignleft, alignright, aligncenter, alignnone)
- `width` - ширина изображения

✅ **Поддержка выравнивания:**
- `alignleft` - изображение слева, текст справа
- `alignright` - изображение справа, текст слева  
- `aligncenter` - изображение по центру
- `alignnone` - без выравнивания

✅ **Адаптивность:**
- Изображения адаптируются под ширину контейнера
- Максимальная ширина сохраняется из оригинального shortcode
- Красивое скругление углов

✅ **Стилизация:**
- Подпись выделена курсивом
- Серый цвет для подписи (#666)
- Отступы для читаемости
- Адаптивные размеры

**Код:**

```php
public static function convertCaptionShortcode($content)
{
    $pattern = '/\[caption[^\]]*\](.*?)\[\/caption\]/is';
    
    $content = preg_replace_callback($pattern, function($matches) {
        $fullMatch = $matches[0];
        $innerContent = $matches[1];
        
        // Извлекаем параметры
        preg_match('/id="([^"]+)"/', $fullMatch, $id);
        preg_match('/align="([^"]+)"/', $fullMatch, $align);
        preg_match('/width="([^"]+)"/', $fullMatch, $width);
        
        // Извлекаем изображение и текст
        preg_match('/<img[^>]+>/i', $innerContent, $img);
        $captionText = trim(strip_tags($innerContent));
        
        // Формируем HTML
        $html = '<figure class="wp-caption ' . $alignClass . '" style="...">';
        $html .= $imgTag;
        $html .= '<figcaption class="wp-caption-text">' . $captionText . '</figcaption>';
        $html .= '</figure>';
        
        return $html;
    }, $content);
    
    return $content;
}
```

#### Обновлён метод `cleanShortcodes()`

Теперь вместо удаления `[caption]` shortcodes, они преобразуются в красивый HTML:

```php
public static function cleanShortcodes($content)
{
    // Преобразуем [caption] в HTML вместо удаления
    $content = self::convertCaptionShortcode($content);
    
    // Удаляем другие shortcodes
    // ...
    
    return $content;
}
```

### 2. Подключён TinyMCE редактор

#### Обновлён файл `resources/views/admin/post-edit.blade.php`

**Добавлено:**

1. **Класс для textarea:**
```html
<textarea class="form-control tinymce-editor" 
          id="post_content" 
          name="post_content">
```

2. **Подключение TinyMCE:**
```html
<script src="{{ asset('vendor/moonshine-tinymce/tinymce.min.js') }}"></script>
```

3. **Инициализация редактора:**
```javascript
tinymce.init({
    selector: '.tinymce-editor',
    language: 'ru',
    language_url: '{{ asset('vendor/moonshine-tinymce/langs/ru.js') }}',
    height: 500,
    menubar: true,
    plugins: [
        'advlist', 'autolink', 'lists', 'link', 'image', 
        'charmap', 'preview', 'anchor', 'searchreplace', 
        'visualblocks', 'code', 'fullscreen', 'insertdatetime', 
        'media', 'table', 'help', 'wordcount'
    ],
    toolbar: 'undo redo | blocks | bold italic forecolor | ...',
    // ... настройки
});
```

---

## 🎨 Функции TinyMCE редактора

### Основные возможности:

✅ **Форматирование текста:**
- Жирный, курсив, подчеркнутый
- Цвет текста
- Выравнивание (слева, по центру, справа, по ширине)

✅ **Списки:**
- Маркированные списки
- Нумерованные списки
- Вложенные списки

✅ **Вставка контента:**
- Изображения
- Ссылки
- Медиа (видео)
- Таблицы

✅ **Дополнительно:**
- Отмена/Повтор действий
- Поиск и замена
- Предпросмотр
- Режим кода (для редактирования HTML)
- Полноэкранный режим
- Подсчет слов

### Настройки стилей:

Редактор настроен так, чтобы контент выглядел в редакторе так же, как на сайте:

```css
body { 
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
    font-size: 16px;
    line-height: 1.6;
    color: #333;
    max-width: 800px;
    margin: 0 auto;
    padding: 20px;
}
img {
    max-width: 100%;
    height: auto;
    border-radius: 5px;
}
figure.wp-caption {
    margin: 20px auto;
    text-align: center;
    max-width: 100%;
}
```

### Русская локализация:

Все элементы интерфейса на русском языке:
- Параграф, Заголовок 2, Заголовок 3, Заголовок 4
- Все кнопки и меню на русском
- Справка на русском

---

## 📊 Примеры использования

### Пример 1: Простой caption

**Shortcode:**
```
[caption id="attachment_123" align="aligncenter" width="800"]
<img src="/imgnews/photo.webp" alt="Фото" width="800" height="600" />
Описание фотографии
[/caption]
```

**Результат:**

<figure class="wp-caption aligncenter" style="margin: 20px auto; text-align: center; max-width: 800px;">
    <img style="width: 100%; height: auto; border-radius: 5px;" src="/imgnews/photo.webp" alt="Фото" />
    <figcaption class="wp-caption-text" style="margin-top: 10px; font-size: 14px; color: #666; font-style: italic;">
        Описание фотографии
    </figcaption>
</figure>

### Пример 2: Caption с выравниванием слева

**Shortcode:**
```
[caption id="attachment_456" align="alignleft" width="400"]
<img src="/imgnews/small.webp" alt="" width="400" height="300" />
Маленькое фото слева
[/caption]
```

**Результат:**

Изображение обтекается текстом справа, выровнено слева.

### Пример 3: Caption без выравнивания

**Shortcode:**
```
[caption id="attachment_789" align="alignnone" width="1200"]
<img src="/imgnews/wide.webp" alt="" width="1200" height="630" />
Широкое изображение без выравнивания
[/caption]
```

**Результат:**

Изображение отображается как блок без обтекания.

---

## ✅ Проверка работы

### 1. Проверка преобразования [caption]

Откройте любую статью со старыми shortcodes:

```bash
# Найти статьи с [caption]
php artisan tinker
>>> $posts = \App\Models\WordPress\Post::where('post_content', 'like', '%[caption%')->get();
>>> $posts->count();
```

Откройте статью на фронтенде - shortcodes должны быть преобразованы в красивый HTML.

### 2. Проверка TinyMCE редактора

1. Перейдите в админку: `http://localhost:8002/notaadmin/posts`
2. Выберите любую статью для редактирования
3. Вы увидите визуальный редактор TinyMCE
4. Попробуйте:
   - Отформатировать текст
   - Вставить изображение
   - Создать список
   - Добавить таблицу
   - Переключиться в режим кода

---

## 🎯 Результат

### До:

❌ Shortcodes отображались как текст  
❌ Некрасивое отображение подписей  
❌ Неудобное редактирование в textarea  
❌ Нужно знать HTML  

### После:

✅ Shortcodes преобразуются в красивый HTML  
✅ Подписи под изображениями выглядят профессионально  
✅ Визуальный редактор TinyMCE  
✅ Не нужно знать HTML  
✅ Русская локализация  
✅ Режим кода для продвинутых пользователей  

---

## 📝 Файлы, которые были изменены

1. **app/Helpers/ContentHelper.php**
   - Добавлен метод `convertCaptionShortcode()`
   - Обновлён метод `cleanShortcodes()`

2. **resources/views/admin/post-edit.blade.php**
   - Добавлен класс `tinymce-editor` к textarea
   - Подключен скрипт TinyMCE
   - Настроена инициализация редактора
   - Добавлены стили

---

## 🚀 Дальнейшие улучшения

### Возможные доработки:

1. **Загрузка изображений через TinyMCE**
   - Реализовать endpoint для загрузки
   - Автоматическая конвертация в WebP
   - Оптимизация размера

2. **Больше shortcodes**
   - `[gallery]` для галерей
   - `[video]` для видео
   - `[quote]` для цитат

3. **Шаблоны контента**
   - Готовые шаблоны статей
   - Быстрая вставка шаблонов

4. **Автосохранение**
   - Сохранение черновиков каждые N секунд
   - Восстановление после сбоя

---

## ✅ Итог

**Обе проблемы успешно решены!**

1. ✅ **Caption shortcodes** преобразуются в красивый HTML
2. ✅ **TinyMCE редактор** подключён и настроен

Теперь:
- Все статьи с `[caption]` отображаются красиво
- Редактирование статей стало удобным
- Не нужно знать HTML для форматирования
- Интерфейс полностью на русском языке

**Готово к использованию! 🎉**

