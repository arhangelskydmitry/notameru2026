# 🎯 ВАЖНО: Обновление v3.0 - HTML-сущности &mdash;

## ❗ Критическое обнаружение

### Проблема
После создания скрипта обнаружилось, что в базе данных длинное тире хранится **НЕ** как символ `—`, а как **HTML-сущность `&mdash;`**.

### Симптомы
- ✅ Скрипт v1.0 и v2.0 показывали "0 статей с тире"
- ❌ Но на сайте в полных текстах видно много длинных тире
- 🔍 В исходном коде HTML: `&mdash;` → браузер отображает как `—`

---

## 🔍 Диагностика

### Проверка статьи #15154:

**В базе данных:**
```php
post_content: "...Киркоров умеет исчезать красиво &mdash; и возвращаться..."
```

**В HTML на сайте:**
```html
...Киркоров умеет исчезать красиво — и возвращаться...
```

**Браузер автоматически преобразует:**
- `&mdash;` (HTML-сущность) → `—` (символ em dash)
- `&nbsp;` → неразрывный пробел
- `&lt;` → `<`
- И т.д.

---

## 📊 Актуальная статистика

### До обновления v3.0:
```
Поиск только символа —:
✅ 0 статей (скрипт ничего не находил)
```

### После обновления v3.0:
```
Поиск символа — и &mdash;:
❌ 245 статей с длинным тире!

Примеры:
- ID: 15079 | 3 тире
- ID: 12359 | 24 тире  
- ID: 14666 | 11 тире
- ID: 14924 | 10 тире
- ID: 14893 | 7 тире
```

---

## 🛠️ Что исправлено в v3.0

### 1. Обновлены SQL запросы

**Было (v1.0/v2.0):**
```sql
WHERE (post_title LIKE '%—%' OR post_content LIKE '%—%' OR post_excerpt LIKE '%—%')
```

**Стало (v3.0):**
```sql
WHERE (post_title LIKE '%—%' OR post_content LIKE '%—%' OR post_excerpt LIKE '%—%'
       OR post_title LIKE '%&mdash;%' OR post_content LIKE '%&mdash;%' OR post_excerpt LIKE '%&mdash;%')
```

### 2. Обновлена функция замены

**Было (v1.0/v2.0):**
```php
$newContent = str_replace('—', '-', $oldContent);
```

**Стало (v3.0):**
```php
$newContent = str_replace(['—', '&mdash;'], '-', $oldContent);
```

### 3. Обновлен подсчет

**Было (v1.0/v2.0):**
```php
$dashesInContent = substr_count($oldContent, '—');
```

**Стало (v3.0):**
```php
$dashesInContent = substr_count($oldContent, '—') + substr_count($oldContent, '&mdash;');
```

---

## ✅ Теперь скрипт заменяет:

| Тип | Было | Станет |
|-----|------|--------|
| Символ | `—` | `-` |
| HTML-сущность | `&mdash;` | `-` |
| Оба варианта | Оба | `-` |

---

## 🚀 Использование v3.0

### 1. Откройте скрипт:
```
http://localhost:8004/replace-dashes.php?key=replace_dashes_2026
```

### 2. Предпросмотр покажет:
```
┌─────────────┬──────────────┬─────────────┬─────────────┬─────────────┐
│ Статей      │ В заголовках │ В контенте  │ В описаниях │ Всего замен │
├─────────────┼──────────────┼─────────────┼─────────────┼─────────────┤
│    245      │      X       │     Y       │      Z      │  ИТОГО      │
└─────────────┴──────────────┴─────────────┴─────────────┴─────────────┘
```

### 3. Выполните замену:
- Нажмите "✅ Выполнить замену (245 статей)"
- Подтвердите действие
- Дождитесь завершения

### 4. Результат:
```
✅ Замена выполнена успешно!

Обновлено статей: 245
Обновлено заголовков: X
Обновлен контент: Y
Обновлено описаний: Z
Всего заменено тире: N
```

---

## 🧪 Проверка после замены

### 1. Через БД:
```sql
-- Должно вернуть 0
SELECT COUNT(*) FROM wp_posts 
WHERE post_type = 'post' 
AND post_status = 'publish'
AND (post_title LIKE '%—%' OR post_content LIKE '%—%' OR post_excerpt LIKE '%—%'
     OR post_title LIKE '%&mdash;%' OR post_content LIKE '%&mdash;%' OR post_excerpt LIKE '%&mdash;%');
```

### 2. Через скрипт:
```
http://localhost:8004/replace-dashes.php?key=replace_dashes_2026
→ "🔍 Предпросмотр"
→ Должно показать: "✅ Длинное тире не найдено!"
```

### 3. На сайте:
```
Откройте статью:
http://localhost:8004/mne-ne-20-i-daze-ne-50-filipp-kirkorov-o-pauze-v-karere-zdorove-i-vozvrashhenii-k-bolsim-sou

Было: ...красиво — и возвращаться...
Стало: ...красиво - и возвращаться...
```

---

## 📋 Changelog

### v3.0 (2026-01-25) - КРИТИЧЕСКОЕ ОБНОВЛЕНИЕ
- ✅ **ИСПРАВЛЕНО:** Добавлена поддержка HTML-сущности `&mdash;`
- ✅ Теперь находит 245 статей (вместо 0)
- ✅ Замена работает для обоих форматов
- ✅ Обновлены SQL запросы
- ✅ Обновлены функции замены и подсчета

### v2.0 (2026-01-25)
- ✅ Добавлена поддержка `post_excerpt`
- ✅ 5 карточек статистики
- ❌ НЕ работало из-за `&mdash;`

### v1.0 (2026-01-24)
- ✅ Первый релиз
- ✅ Поддержка `post_title` и `post_content`
- ❌ НЕ работало из-за `&mdash;`

---

## ⚠️ ВАЖНО

### Почему v1.0 и v2.0 не работали:
1. Искали только символ `—`
2. В БД хранятся HTML-сущности `&mdash;`
3. Скрипт не находил совпадений (0 статей)
4. На сайте тире видны, т.к. браузер преобразует `&mdash;` в `—`

### Почему v3.0 работает:
1. ✅ Ищет оба варианта: `—` и `&mdash;`
2. ✅ Заменяет оба варианта на `-`
3. ✅ Находит 245 статей
4. ✅ После замены на сайте будет `-`

---

## 🎯 Рекомендация

**Используйте только v3.0!**

Предыдущие версии (v1.0, v2.0) не работают для вашей базы данных, т.к. не обрабатывают HTML-сущности.

---

## 📞 Техническая информация

**Формат хранения в БД:** HTML-сущности  
**Причина:** WordPress автоматически конвертирует специальные символы  
**Решение:** Скрипт v3.0 с поддержкой `&mdash;`

**Файлы:**
- `replace-dashes.php` (корень) - v3.0
- `public/replace-dashes.php` - v3.0
- `REPLACE_DASHES_V3_CRITICAL.md` - этот документ

---

✅ **v3.0 ГОТОВ К ИСПОЛЬЗОВАНИЮ!**

**245 статей ждут замены!** 🚀
