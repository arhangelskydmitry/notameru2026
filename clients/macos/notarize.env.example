# Скопируйте в notarize.env и заполните:
# cp notarize.env.example notarize.env

# Профиль notarytool (создаётся один раз):
# xcrun notarytool store-credentials "NotaMiru-Notary" \
#   --apple-id "YOUR_APPLE_ID" \
#   --team-id "R4YV9N2RNS" \
#   --password "APP-SPECIFIC-PASSWORD"

NOTARY_PROFILE="NotaMiru-Notary"
