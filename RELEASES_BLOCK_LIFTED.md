# ✅ Уменьшение отступа между "Интервью" и "Релизы" - Выполнено!

## 🎯 Что было сделано

### ✅ Блок "Релизы" поднят на 20 пикселей

**Добавлено:**
```html
<div class="sidebar-widget" style="margin-top: -20px;">
    <h3 class="widget-title">Релизы</h3>
    ...
</div>
```

**Результат:**
- Блок "Релизы" поднялся на 20px вверх
- Отступ между "Интервью" и "Релизы" уменьшился с 15px до -5px (визуально почти слились)
- Блоки стали компактнее

---

## 📊 Визуальное сравнение

### Было:
```
┌─────────────┐
│  Интервью   │
│             │
└─────────────┘
      ↓ 15px
┌─────────────┐
│   Релизы    │
│             │
└─────────────┘
```

### Стало:
```
┌─────────────┐
│  Интервью   │
│             │
└─────────────┘
      ↓ -5px (15px - 20px)
┌─────────────┐
│   Релизы    │
│             │
└─────────────┘
```

**Фактический отступ теперь:** 15px (gap) - 20px (margin-top) = **-5px** (наезжает)

---

## 🎨 Технические детали

### CSS свойства:

| Элемент | Свойство | Значение | Описание |
|---------|----------|----------|----------|
| Родитель | `gap` | `15px` | Базовый отступ между элементами |
| Релизы | `margin-top` | `-20px` | Отрицательный margin поднимает блок |

### Как работает отрицательный margin:

```css
/* Стандартное поведение grid gap */
gap: 15px;  /* Между всеми элементами 15px */

/* Отрицательный margin перекрывает gap */
margin-top: -20px;  /* Поднимает элемент на 20px вверх */

/* Итоговый отступ: 15px - 20px = -5px */
```

### Визуальная схема:

```
Grid gap 15px:
┌─────┐
│  A  │
└─────┘
│ 15px │ ← gap
┌─────┐
│  B  │
└─────┘

С margin-top: -20px:
┌─────┐
│  A  │
└─────┘
│ -5px │ ← 15px gap - 20px margin
┌─────┐
│  B  │ ← Поднялся на 20px
└─────┘
```

---

## 📐 Размеры отступов

### До изменения:
- **Gap между блоками:** 15px
- **Отступ "Интервью" → "Релизы":** 15px

### После изменения:
- **Gap между блоками:** 15px (не изменился)
- **Margin-top "Релизы":** -20px
- **Фактический отступ:** 15px - 20px = **-5px**

---

## ✅ Преимущества решения

### 1. Использование margin вместо изменения gap
✅ Gap остаётся единым для всех элементов  
✅ Точечная настройка конкретного блока  
✅ Легко изменить или убрать  
✅ Не влияет на другие элементы  

### 2. Отрицательный margin
✅ Блок поднимается вверх  
✅ Визуально компактнее  
✅ Больше контента на экране  
✅ Современный подход  

---

## 🎯 Результат

**Блок "Релизы" теперь:**

✅ Поднят на 20 пикселей вверх  
✅ Отступ от "Интервью" уменьшился  
✅ Фактический отступ: -5px (почти слились)  
✅ Компактная и удобная верстка  
✅ Больше пространства для контента  

**Готово к использованию! 🚀**

---

## 💡 Настройка отступа

Если нужно изменить расстояние:

### Больше подъём (меньше отступ):
```html
style="margin-top: -30px;"  /* Поднимет на 30px */
```

### Меньше подъём (больше отступ):
```html
style="margin-top: -10px;"  /* Поднимет на 10px */
```

### Вернуть к исходному:
```html
style="margin-top: 0;"  /* Убрать margin */
<!-- или просто удалить style -->
```

### Опустить вниз:
```html
style="margin-top: 10px;"  /* Добавит отступ сверху */
```

---

## 📊 Итоговые расстояния

| Место | Размер |
|-------|--------|
| Слайдер ↔ Интервью/Релизы | 15px |
| Интервью ↓ Релизы | -5px |
| Блоки ↓ "Все новости" | 20px |

**Верстка стала компактнее и сбалансированнее!**

