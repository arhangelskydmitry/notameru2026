# 🔄 Руководство по миграции данных WordPress → Laravel

**Дата:** 5 ноября 2025  
**Phase 6 готова к выполнению**

---

## 📊 Обзор

Этот документ описывает процесс переноса данных из WordPress таблиц в новые Laravel таблицы.

**Что будет перенесено:**
- 17 категорий
- 896 тегов
- 2,464 поста (с SEO, миниатюрами, связями)

---

## 🗄️ Новая структура БД

### Таблица `posts`

| Поле | Тип | Описание |
|------|-----|----------|
| id | bigint | Первичный ключ |
| title | string | Заголовок |
| slug | string | URL slug (unique) |
| excerpt | text | Краткое описание |
| content | longtext | Полный контент |
| status | enum | draft/published/pending |
| author_id | bigint | FK на users |
| featured_image | string | URL миниатюры |
| views | int | Счётчик просмотров |
| **SEO поля:** | | |
| meta_title | string | SEO заголовок |
| meta_description | text | SEO описание |
| meta_keywords | string | Ключевые слова |
| canonical_url | string | Канонический URL |
| **Open Graph:** | | |
| og_title, og_description, og_image | | Facebook |
| **Twitter Card:** | | |
| twitter_title, twitter_description, twitter_image | | Twitter |
| timestamps | | created_at, updated_at |
| deleted_at | timestamp | Soft delete |

**Индексы:**
- status, author_id, created_at
- Fulltext: title, content
- Unique: slug

---

### Таблица `categories`

| Поле | Тип | Описание |
|------|-----|----------|
| id | bigint | Первичный ключ |
| name | string | Название |
| slug | string | URL slug (unique) |
| description | text | Описание |
| parent_id | bigint | FK на categories (иерархия) |
| order | int | Порядок сортировки |
| timestamps | | created_at, updated_at |

---

### Таблица `tags`

| Поле | Тип | Описание |
|------|-----|----------|
| id | bigint | Первичный ключ |
| name | string | Название |
| slug | string | URL slug (unique) |
| description | text | Описание |
| timestamps | | created_at, updated_at |

---

### Связующие таблицы

**post_category** - many-to-many посты ↔ категории  
**post_tag** - many-to-many посты ↔ теги

---

## 🚀 Процесс миграции

### Шаг 1: Создать новые таблицы

```bash
cd /Users/mac/Sites/notamerularavel
php artisan migrate
```

Это создаст новые таблицы posts, categories, tags и связующие.

---

### Шаг 2: Перенести данные

```bash
# Перенести всё сразу
php artisan migrate:wordpress --type=all

# Или по частям:
php artisan migrate:wordpress --type=categories
php artisan migrate:wordpress --type=tags
php artisan migrate:wordpress --type=posts
```

**Процесс:**
1. Переносит категории (17 шт)
2. Переносит теги (896 шт)
3. Переносит посты (2,464 шт) с:
   - Всеми полями
   - SEO данными
   - Миниатюрами
   - Связями с категориями и тегами
   - Счётчиками просмотров

**Время выполнения:** ~5-10 минут

---

### Шаг 3: Проверить данные

```bash
php artisan tinker
```

```php
// Проверить посты
\App\Models\LaravelPost::count(); // Должно быть 2464

// Проверить категории
\App\Models\LaravelCategory::count(); // Должно быть 17

// Проверить теги
\App\Models\LaravelTag::count(); // Должно быть 896

// Проверить связи
$post = \App\Models\LaravelPost::with(['categories', 'tags'])->first();
$post->categories; // Категории поста
$post->tags; // Теги поста
```

---

### Шаг 4: Обновить фронтенд (опционально)

Если хотите использовать новые модели вместо WordPress:

1. Замените `App\Models\WordPress\Post` на `App\Models\LaravelPost` в контроллерах
2. Обновите Moonshine Resources для работы с новыми таблицами
3. Протестируйте фронтенд

---

## ⚠️ Важно!

### Безопасность:

✅ **WordPress данные НЕ удаляются**  
✅ **Только копирование в новые таблицы**  
✅ **WordPress продолжит работать**  
✅ **Можно откатить в любой момент**

### Rollback:

Если что-то пойдёт не так:

```bash
# Удалить новые таблицы
php artisan migrate:rollback

# Или очистить данные
php artisan tinker
\App\Models\LaravelPost::truncate();
\App\Models\LaravelCategory::truncate();
\App\Models\LaravelTag::truncate();
```

WordPress останется нетронутым!

---

## 🎯 После миграции

### Вариант 1: Гибридный (Рекомендуется)
- Оставить обе системы
- WordPress для фронтенда
- Laravel для новых фичей
- **Плюс:** Безопасно, гибко

### Вариант 2: Полный переход
- Переключить фронтенд на Laravel модели
- Отключить WordPress
- **Плюс:** Одна система

---

## 📚 Mapping WordPress → Laravel

| WordPress | Laravel | Комментарий |
|-----------|---------|-------------|
| wp_posts (post_type=post) | posts | Только опубликованные |
| wp_terms + wp_term_taxonomy (category) | categories | С parent_id |
| wp_terms + wp_term_taxonomy (post_tag) | tags | Простая структура |
| wp_term_relationships | post_category, post_tag | Many-to-many |
| wp_postmeta (Yoast SEO) | posts.meta_* | Прямо в таблице |

---

## ✅ Преимущества новой структуры

1. **Простота** - одна таблица вместо 5
2. **Скорость** - меньше JOIN'ов
3. **SEO** - всё в одной таблице
4. **Современность** - Laravel стандарты
5. **Расширяемость** - легко добавлять поля

---

## 🎊 Итог

Phase 6 подготовлена полностью!

Все инструменты готовы для миграции 9,775 записей.

**Выполнение миграции - опционально!**  
Сайт уже работает с WordPress данными.

---

**Создано:** 5 ноября 2025  
**Статус:** Готово к выполнению ✅

